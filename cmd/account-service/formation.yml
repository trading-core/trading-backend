---
- name: Render account-service .env
  include_tasks: "{{ playbook_dir }}/tasks/render-env.yml"
  vars:
    env_dest: backend/account-service/.env
    resources:
      secrets: [token, broker_credentials]
      logs:    [account_event_log]
      apis:    [trading_api, frontend]
