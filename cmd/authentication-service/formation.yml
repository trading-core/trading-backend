---
- name: Render authentication-service .env
  include_tasks: "{{ playbook_dir }}/tasks/render-env.yml"
  vars:
    env_dest: backend/authentication-service/.env
    resources:
      secrets: [token]
      stores:  [user_postgres]
