---
- name: Render bot-service .env
  include_tasks: "{{ playbook_dir }}/tasks/render-env.yml"
  vars:
    env_dest: backend/bot-service/.env
    resources:
      secrets: [token, broker_credentials]
      logs:    [bot_event_log]
      clients: [account_service]
