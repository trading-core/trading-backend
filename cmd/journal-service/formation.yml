---
- name: Render journal-service .env
  include_tasks: "{{ playbook_dir }}/tasks/render-env.yml"
  vars:
    env_dest: backend/journal-service/.env
    resources:
      secrets: [token]
      logs:    [journal_event_log]
