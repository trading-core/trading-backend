---
- name: Render stock-screener .env
  include_tasks: "{{ playbook_dir }}/tasks/render-env.yml"
  vars:
    env_dest: backend/stock-screener/.env
    resources:
      secrets: [token, alpaca]
      apis:    [alpaca, cnn_market_sentiment]
