---
- name: Render storage-service .env
  include_tasks: "{{ playbook_dir }}/tasks/render-env.yml"
  vars:
    env_dest: backend/storage-service/.env
    resources:
      secrets: [token]
      logs:    [storage_event_log]
      storage: [filesystem]
